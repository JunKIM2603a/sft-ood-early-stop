name: sft-ood-es

channels:
  - conda-forge

dependencies:
  - python=3.11
  - pip
  - git
  - git-lfs
  - cmake
  - ninja
  - pip:
      - --extra-index-url https://download.pytorch.org/whl/cu126
      - torch==2.7.1+cu126
      - transformers==5.17.0
      - datasets==5.0.1
      - peft==0.21.0
      - accelerate==1.15.0
      - huggingface-hub==1.32.0
      - numpy
      - pandas
      - pyarrow
      - scipy
      - scikit-learn
      - sympy
      - PyYAML
      - tqdm
      - rich
      - matplotlib
      - safetensors
      - sentencepiece
      - protobuf
      - tensorboard
      - wandb
      - pytest
      - pytest-cov
      - ruff
